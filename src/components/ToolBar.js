import styled from 'styled-components';

const ToolBar = styled.div`
  width: 100%;
  background: #fff;
  box-shadow: 0 0 20px rgba(0, 0, 0, 0.4);
  height: 38.5px;

  li {
    list-style: none;
  }

  h1 {
    text-align: center;
    color: #f1a46f;
  }
`;

export { ToolBar };
